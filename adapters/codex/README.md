# Codex adapter

Run `node scripts/install-global.mjs --target codex` from the repository to install the self-contained router into `$CODEX_HOME/skills`, defaulting to `~/.codex/skills`. Keep upstream skills separately installed so Codex can load only the selected capabilities.
