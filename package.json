{
  "name": "bundle-useful-skills",
  "version": "0.3.0",
  "private": true,
  "description": "A small, agent-neutral skill router for engineering, marketing, brand, content, automation, and venture work",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "route": "node scripts/cli.mjs route",
    "validate": "node scripts/validate-registry.mjs",
    "credits": "node scripts/generate-credits.mjs",
    "install:global": "node scripts/install-global.mjs --target all",
    "doctor": "node scripts/doctor.mjs --target all",
    "license-check": "node scripts/license-check.mjs",
    "secret-check": "node scripts/secret-check.mjs",
    "check": "npm test && npm run validate && npm run license-check && node scripts/generate-credits.mjs --check && npm run secret-check"
  }
}
