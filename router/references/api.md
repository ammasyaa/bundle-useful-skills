# API contract routing

Define ownership, authentication and authorization, inputs, outputs, errors, pagination, idempotency, concurrency, versioning, and compatibility before changing an API. Test the observable contract and failure modes. Load the client framework only if client code changes.
