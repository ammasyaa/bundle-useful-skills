# Release gate

Use fresh evidence. Run project tests, static analysis, packaging/build checks, and platform-specific distribution checks. Confirm configuration, migrations, permissions, signing, installers, upgrades, rollback or recovery, observability, accessibility, and sensitive-data handling where applicable. Report skipped checks and their impact. Never describe a build as production ready based only on source inspection.
